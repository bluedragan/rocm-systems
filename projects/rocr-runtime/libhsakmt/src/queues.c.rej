--- libhsakmt/src/queues.c
+++ libhsakmt/src/queues.c
@@ -521,7 +539,7 @@ static int handle_concrete_asic(struct queue *q,
 
 	if (q->eop_buffer_size > 0) {
 		pr_info("Allocating VRAM for EOP\n");
-		q->eop_buffer = allocate_exec_aligned_memory(q->eop_buffer_size,
+		q->eop_buffer = allocate_exec_aligned_memory_eop(q->eop_buffer_size,
 				q->use_ats, gpu_id,
 				NodeId, true, true, /* Unused for VRAM */false);
 		if (!q->eop_buffer)
